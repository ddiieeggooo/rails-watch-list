<h1>Movie Lists</h1>
<ul>
<% @lists.each do |list| %>
<div class="card">
<div class="card-body">
<li> <%= list.id %> <%= link_to list.name, list_path(list.id) %></li>
</div>
</div>
<% end %>
</ul>
<%= link_to "create a new list", new_list_path %>

<%= cl_image_tag("z5krht1azvtgnnyo5amk",
      width: 400, height: 300, crop: :fill) %>
